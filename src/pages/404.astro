---
import Layout from "@/layouts/Layout.astro";
---

<Layout title="404 - Pagina no encontrada">
    <main class="flex flex-col justify-center items-start w-full h-full text-white p-4 max-w-3xl mx-auto">
        <div class="text-8xl font-light mb-10 leading-none">:(</div>
        
        <div class="max-w-3xl shrink-0">
            <p class="text-lg font-light leading-relaxed mb-6 tracking-wide">
                No se pudo cargar la página solicitada, puede que la ruta no exista o que haya ocurrido un error 
                <span class="hidden lg:inline">
                    , estamos recopilando información sobre el problema, vuelve a la página principal presionando cualquier tecla.
                </span>
            </p>
            
            <div class="mb-6">
                <p class="font-light leading-relaxed opacity-95 tracking-wide w-full">
                    Tipo de error: 404 — Pagina no encontrada
                </p>
                <p class="font-light leading-relaxed opacity-95 tracking-wide w-full mt-2">
                    Error: PAGINA_NO_ENCONTRADA
                </p>
            </div>

            <a href="/"
               aria-label="Volver al inicio"
               class="block lg:hidden w-max text-center px-6 py-3 bg-white text-blue-600 rounded-md font-medium hover:bg-gray-100 transition">
                Volver al inicio
            </a>

            <p class="hidden lg:block text-xl font-light tracking-wide">
                Presiona cualquier tecla para continuar...
            </p>
        </div>
    </main>
</Layout>

<style>
    html,
    body {
        font-family: "Segoe UI", Arial, sans-serif;
        background: linear-gradient(135deg, #0254c1 0%, #1084d7 100%);
        height: 100dvh;
        overflow: hidden;
    }
</style>

<script type="module">
    if (window.innerWidth >= 1024) {
        document.addEventListener('keydown', () => {
            window.location.href = '/';
        }, { once: true });
    }
</script>